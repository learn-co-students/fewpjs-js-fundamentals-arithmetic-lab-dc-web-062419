let num1 = 2
let num2 = 31

var multiply = (num1 * num2);

var random = Math.floor(Math.random() * 10) + 1;

// set to an equation whose remainder is equal to 4
var mod = 32 % 14

// that will return 20 as the higest number in the set
var max = Math.max(1, 4, 6, 8, 20)